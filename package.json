{
  "name": "pinky-super-repo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch_repos.js",
    "validate": "node scripts/validate_repos.js",
    "dashboard": "node scripts/generate_dashboard.js",
    "scan": "npm run fetch && npm run validate && npm run dashboard"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "node-fetch": "^3.3.2"
  }
}
